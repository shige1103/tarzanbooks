<% if logged_in? %>
 <div class="center jumbotron">
    <div class="text-center">
      <h1>My Portfolio</h1>
      <h1>～自己紹介＆写真共有ページ～</h1>
        <%= link_to 'Profile', user_path(current_user), class: 'btn btn-primary' %>
    </div>
  </div>
  <div class="center jumbotron">
    <div class="text-center">
      <h2>PHOTOS</h2>
    </div>
    <div class="text-center">
      <%= link_to '写真アップロード', new_post_path(@user),class: 'btn btn-primary' %>
    </div>
  </div>
  <div class="center jumbotron">
    <div class="text-center">
      <h2>PHOTOS LIST</h2>
    </div>
    <div class="text-center">
      <%= link_to 'アルバム画面へ', post_path(current_user),class: 'btn btn-primary' %>
  </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>My Portfolio</h1>
      <h1>～自己紹介＆写真共有ページ～</h1>
      <%= link_to 'ログイン', login_path, class: 'btn btn-primary btn-block' %></li>
    </div>
  </div>
<% end %>